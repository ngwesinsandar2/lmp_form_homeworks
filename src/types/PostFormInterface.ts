export interface LMPPostFormInterface {
  name: string,
  gender: string,
  city: string,
  phone: string,
  note: string,
}

export interface MyPostFormInterface {
  name: string,
  email: string,
  note: string,
}